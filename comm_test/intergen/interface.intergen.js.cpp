
//@file  javascript interface dispatcher generated by intergen v8
//See LICENSE file for copyright and license information

#include "ifc/js/client.h"
#include "ifc/js/client2.h"
#include "interface.hpp"

#include <comm/intergen/ifc.js.h>
#include <comm/metastream/metastream.h>
#include <comm/metastream/fmtstream_v8.h>
#include <comm/binstream/filestream.h>
#include <comm/binstream/binstreambuf.h>

using namespace coid;

////////////////////////////////////////////////////////////////////////////////
//
// javascript handler of interface client of class ab::cd::host
//
////////////////////////////////////////////////////////////////////////////////

namespace js {

////////////////////////////////////////////////////////////////////////////////
class client_js_dispatcher
    : public ::js::interface_wrapper_base<::client>
{
    mutable v8::Persistent<v8::Function> _events[3];
    mutable bool _bound_events;

    static void _js_release_callback(const v8::WeakCallbackInfo<client_js_dispatcher>& data)
    {
        client_js_dispatcher* ifc = data.GetParameter();
        ifc->_object.Reset();
        ifc->release_refcount();
    }

protected:

    backend intergen_backend() const override { return backend::js; }

public:

    v8::Handle<v8::Object> create_interface_object(v8::Handle<v8::Context> context, bool make_weak);

    static v8::Handle<v8::Script> load_script(const coid::token& script, const coid::token& file_name);
    void bind_events(v8::Handle<v8::Context> context, bool force, v8::Handle<v8::Value> ref) const;

    COIDNEWDELETE(client_js_dispatcher);

    client_js_dispatcher() : _bound_events(false)
    {}

    explicit client_js_dispatcher(client* orig) : _bound_events(false) {
        _base.create(orig);
        _vtable = _base->vtable();

        _host = _base->host<policy_intrusive_base>();
    }

    explicit client_js_dispatcher(policy_intrusive_base* host) : _bound_events(false) {
        set_host(host, this, 0);
    }

    ~client_js_dispatcher()
    {
        v8::Isolate* iso = v8::Isolate::GetCurrent();
        v8::HandleScope handle_scope__(iso);

        for (int i=0; i<3; ++i) {
            _events[i].Reset();
        }

        if (!_object.IsEmpty()) {
            _object.Get(iso)->SetInternalField(0, v8::Null(iso));
        }

        _object.Reset();
    }

    bool intergen_bind_capture(coid::binstring* capture, uint instid) override
    {
        return _real()->intergen_bind_capture(capture, instid);
    }

    void intergen_capture_dispatch(uint mid, coid::binstring& bin) override
    {
        _real()->intergen_capture_dispatch(mid, bin);
    }

    // --- creators ---

    /// @brief interface creator
    /// @return host class instance
    static iref<client_js_dispatcher> creator(const ::js::script_handle& scriptpath, const coid::token& bindname, v8::Handle<v8::Context>*);

    static v8::Handle<v8::Value> v8creator_creator0(const v8::ARGUMENTS& args);

    static void v8rebind_events(const v8::ARGUMENTS& args);
    static void v8current_global(const v8::ARGUMENTS& args);

    // --- method wrappers ---

    static void v8_set_def0(const v8::ARGUMENTS& args);
    static void v8_set1(const v8::ARGUMENTS& args);
    static void v8_get2(const v8::ARGUMENTS& args);
    static void v8_custom3(const v8::ARGUMENTS& args);
    static void v8_overridable4(const v8::ARGUMENTS& args);

    // --- interface events ---

    virtual void echo( int k ) override;
    virtual void callforth( void (*cbk)(int, const coid::token&) ) override;
    virtual int required( int x ) override;

    virtual bool is_bound(event m) override {
        v8::Isolate* iso = v8::Isolate::GetCurrent();
        if (!_bound_events) {
            v8::HandleScope handle_scope__(iso);
            v8::TryCatch js_trycatch(iso);

            bind_events(context(iso), false, v8::Undefined(iso));
        }

        int id = int(m);
        return !_events[id].IsEmpty() && !_events[id].Get(iso)->IsUndefined();
    }

    virtual void force_bind_script_events() {
        v8::Isolate* iso = v8::Isolate::GetCurrent();
        v8::HandleScope handle_scope__(iso);
        v8::TryCatch js_trycatch(iso);

        bind_events(context(iso), true, v8::Local<v8::Value>());
    }

private:

    //event wrappers for event invocation from js
    static void v8_evback_echo0(const v8::ARGUMENTS& args);
    static void v8_evback_callforth1(const v8::ARGUMENTS& args);
    static void v8_evback_required2(const v8::ARGUMENTS& args);
};

////////////////////////////////////////////////////////////////////////////////
void client_js_dispatcher::v8_set_def0(const v8::ARGUMENTS& args)
{
    v8::Isolate* iso = args.GetIsolate();

    if (args.Length() < 0 || args.Length() > 1) { //in/inout arguments
        coid::charstr tmp = "Wrong number of arguments in ";
        tmp << "client.set_def";
        return v8::queue_js_exception(iso, v8::Exception::SyntaxError, tmp);
    }

    v8::HandleScope handle_scope__(iso);
    v8::Local<v8::Object> obj__ = args.Holder();
    if (obj__.IsEmpty() || obj__->InternalFieldCount() == 0)
        return (void)args.GetReturnValue().Set(v8::Undefined(iso));

    v8::Local<v8::Value> intobj__ = obj__->GetInternalField(0);
    if (!intobj__->IsExternal())
        return (void)args.GetReturnValue().Set(v8::Undefined(iso));

    ::js::interface_wrapper_base<::client>* ifc = static_cast<::js::interface_wrapper_base<::client>*>
        (v8::Handle<v8::External>::Cast(intobj__)->Value());

    auto R_ = ifc ? ifc->_real() : 0;
    if (!R_) {
        coid::charstr tmp = "Null interface object in ";
        tmp << "client.set_def";
        return v8::queue_js_exception(iso, &v8::Exception::ReferenceError, tmp);
    }

    v8::Local<v8::Context> ctx = ifc->context(iso);
    v8::Context::Scope context_scope(ctx);

    try {
        //stream the arguments in
        static_assert(coid::has_metastream_operator<flags>::value, "missing metastream operator for 'flags'");

        threadcached<flags> flg;
        if (0 < args.Length())
            write_from_v8(args[0], flg);
        else
            flg = {.a = 1, .b = 2};

        //invoke
        R_->set_def(flg);

        //stream out
        v8::Handle<v8::Object> r__;

        args.GetReturnValue().Set(r__);
    }
    catch (const coid::exception& e) {
        return v8::queue_js_exception(iso, &v8::Exception::TypeError, e.text());
    }
}

////////////////////////////////////////////////////////////////////////////////
void client_js_dispatcher::v8_set1(const v8::ARGUMENTS& args)
{
    v8::Isolate* iso = args.GetIsolate();

    if (args.Length() < 1 || args.Length() > 1) { //in/inout arguments
        coid::charstr tmp = "Wrong number of arguments in ";
        tmp << "client.set";
        return v8::queue_js_exception(iso, v8::Exception::SyntaxError, tmp);
    }

    v8::HandleScope handle_scope__(iso);
    v8::Local<v8::Object> obj__ = args.Holder();
    if (obj__.IsEmpty() || obj__->InternalFieldCount() == 0)
        return (void)args.GetReturnValue().Set(v8::Undefined(iso));

    v8::Local<v8::Value> intobj__ = obj__->GetInternalField(0);
    if (!intobj__->IsExternal())
        return (void)args.GetReturnValue().Set(v8::Undefined(iso));

    ::js::interface_wrapper_base<::client>* ifc = static_cast<::js::interface_wrapper_base<::client>*>
        (v8::Handle<v8::External>::Cast(intobj__)->Value());

    auto R_ = ifc ? ifc->_real() : 0;
    if (!R_) {
        coid::charstr tmp = "Null interface object in ";
        tmp << "client.set";
        return v8::queue_js_exception(iso, &v8::Exception::ReferenceError, tmp);
    }

    v8::Local<v8::Context> ctx = ifc->context(iso);
    v8::Context::Scope context_scope(ctx);

    try {
        //stream the arguments in
        static_assert(coid::has_metastream_operator<coid::token>::value, "missing metastream operator for 'coid::token'");

        threadcached<coid::token> par;
        write_from_v8(args[0], par);

        //invoke
        R_->set(par);

        //stream out
        v8::Handle<v8::Object> r__;

        args.GetReturnValue().Set(r__);
    }
    catch (const coid::exception& e) {
        return v8::queue_js_exception(iso, &v8::Exception::TypeError, e.text());
    }
}

////////////////////////////////////////////////////////////////////////////////
void client_js_dispatcher::v8_get2(const v8::ARGUMENTS& args)
{
    v8::Isolate* iso = args.GetIsolate();

    if (args.Length() < 0 || args.Length() > 0) { //in/inout arguments
        coid::charstr tmp = "Wrong number of arguments in ";
        tmp << "client.get";
        return v8::queue_js_exception(iso, v8::Exception::SyntaxError, tmp);
    }

    v8::HandleScope handle_scope__(iso);
    v8::Local<v8::Object> obj__ = args.Holder();
    if (obj__.IsEmpty() || obj__->InternalFieldCount() == 0)
        return (void)args.GetReturnValue().Set(v8::Undefined(iso));

    v8::Local<v8::Value> intobj__ = obj__->GetInternalField(0);
    if (!intobj__->IsExternal())
        return (void)args.GetReturnValue().Set(v8::Undefined(iso));

    ::js::interface_wrapper_base<::client>* ifc = static_cast<::js::interface_wrapper_base<::client>*>
        (v8::Handle<v8::External>::Cast(intobj__)->Value());

    auto R_ = ifc ? ifc->_real() : 0;
    if (!R_) {
        coid::charstr tmp = "Null interface object in ";
        tmp << "client.get";
        return v8::queue_js_exception(iso, &v8::Exception::ReferenceError, tmp);
    }

    v8::Local<v8::Context> ctx = ifc->context(iso);
    v8::Context::Scope context_scope(ctx);

    try {
        //stream the arguments in


        //invoke
        coid::charstr par;
        int _rval_ = R_->get(par);

        //stream out
        static_assert(coid::has_metastream_operator<int>::value, "missing metastream operator for 'int'");
        v8::Handle<v8::Object> r__ = v8::Object::New(iso);
        r__->Set(ctx, v8::symbol("$ret", iso), read_to_v8(_rval_));

        static_assert(coid::has_metastream_operator<coid::charstr>::value, "missing metastream operator for 'coid::charstr'");
        r__->Set(ctx, v8::symbol("par", iso), read_to_v8(par));
        args.GetReturnValue().Set(r__);
    }
    catch (const coid::exception& e) {
        return v8::queue_js_exception(iso, &v8::Exception::TypeError, e.text());
    }
}

////////////////////////////////////////////////////////////////////////////////
void client_js_dispatcher::v8_custom3(const v8::ARGUMENTS& args)
{
    v8::Isolate* iso = args.GetIsolate();

    if (args.Length() < 0 || args.Length() > 0) { //in/inout arguments
        coid::charstr tmp = "Wrong number of arguments in ";
        tmp << "client.custom";
        return v8::queue_js_exception(iso, v8::Exception::SyntaxError, tmp);
    }

    v8::HandleScope handle_scope__(iso);
    v8::Local<v8::Object> obj__ = args.Holder();
    if (obj__.IsEmpty() || obj__->InternalFieldCount() == 0)
        return (void)args.GetReturnValue().Set(v8::Undefined(iso));

    v8::Local<v8::Value> intobj__ = obj__->GetInternalField(0);
    if (!intobj__->IsExternal())
        return (void)args.GetReturnValue().Set(v8::Undefined(iso));

    ::js::interface_wrapper_base<::client>* ifc = static_cast<::js::interface_wrapper_base<::client>*>
        (v8::Handle<v8::External>::Cast(intobj__)->Value());

    auto R_ = ifc ? ifc->_real() : 0;
    if (!R_) {
        coid::charstr tmp = "Null interface object in ";
        tmp << "client.custom";
        return v8::queue_js_exception(iso, &v8::Exception::ReferenceError, tmp);
    }

    v8::Local<v8::Context> ctx = ifc->context(iso);
    v8::Context::Scope context_scope(ctx);

    try {
        //stream the arguments in


        //invoke
        sometype _rval_ = R_->custom();

        //stream out
        v8::Handle<v8::Value> r__;
        static_assert(coid::has_metastream_operator<sometype>::value, "missing metastream operator for 'sometype'");
        r__ = read_to_v8(_rval_);

        args.GetReturnValue().Set(r__);
    }
    catch (const coid::exception& e) {
        return v8::queue_js_exception(iso, &v8::Exception::TypeError, e.text());
    }
}

////////////////////////////////////////////////////////////////////////////////
void client_js_dispatcher::v8_overridable4(const v8::ARGUMENTS& args)
{
    v8::Isolate* iso = args.GetIsolate();

    if (args.Length() < 0 || args.Length() > 0) { //in/inout arguments
        coid::charstr tmp = "Wrong number of arguments in ";
        tmp << "client.overridable";
        return v8::queue_js_exception(iso, v8::Exception::SyntaxError, tmp);
    }

    v8::HandleScope handle_scope__(iso);
    v8::Local<v8::Object> obj__ = args.Holder();
    if (obj__.IsEmpty() || obj__->InternalFieldCount() == 0)
        return (void)args.GetReturnValue().Set(v8::Undefined(iso));

    v8::Local<v8::Value> intobj__ = obj__->GetInternalField(0);
    if (!intobj__->IsExternal())
        return (void)args.GetReturnValue().Set(v8::Undefined(iso));

    ::js::interface_wrapper_base<::client>* ifc = static_cast<::js::interface_wrapper_base<::client>*>
        (v8::Handle<v8::External>::Cast(intobj__)->Value());

    auto R_ = ifc ? ifc->_real() : 0;
    if (!R_) {
        coid::charstr tmp = "Null interface object in ";
        tmp << "client.overridable";
        return v8::queue_js_exception(iso, &v8::Exception::ReferenceError, tmp);
    }

    v8::Local<v8::Context> ctx = ifc->context(iso);
    v8::Context::Scope context_scope(ctx);

    try {
        //stream the arguments in


        //invoke
        bool _rval_ = R_->overridable();

        //stream out
        v8::Handle<v8::Value> r__;
        static_assert(coid::has_metastream_operator<bool>::value, "missing metastream operator for 'bool'");
        r__ = read_to_v8(_rval_);

        args.GetReturnValue().Set(r__);
    }
    catch (const coid::exception& e) {
        return v8::queue_js_exception(iso, &v8::Exception::TypeError, e.text());
    }
}

////////////////////////////////////////////////////////////////////////////////
void client_js_dispatcher::echo( int k )
{
    v8::Isolate* iso = v8::Isolate::GetCurrent();

    v8::HandleScope handle_scope__(iso);
    v8::Handle<v8::Context> ctx__ = context(iso);
    v8::Context::Scope context_scope__(ctx__);
    v8::TryCatch js_trycatch__(iso);

    bind_events(ctx__, false, v8::Undefined(iso));
    auto ev__ = _events[0].Get(iso);

    if (_events[0].IsEmpty() || ev__->IsUndefined())
        return;

// prepare in arguments
    static_assert(coid::has_metastream_operator<int>::value, "missing metastream operator for 'int'");
    v8::Handle<v8::Value> __inargs[] = {
        to_v8<int>::read(k)
    };

//  event call
    v8::Local<v8::Value> r__ = ev__->Call(
        ctx__,
        _object.Get(iso),
        1, __inargs).ToLocalChecked();


    if (js_trycatch__.HasCaught())
        ::js::script_handle::throw_exception_from_js_error(js_trycatch__, "js::client.echo");
// gather results

}

////////////////////////////////////////////////////////////////////////////////
void client_js_dispatcher::v8_evback_echo0(const v8::ARGUMENTS& args)
{
    v8::Isolate* iso = args.GetIsolate();

    v8::HandleScope handle_scope__(iso);
    v8::Local<v8::Object> obj__ = args.Holder();
    if (obj__.IsEmpty() || obj__->InternalFieldCount() == 0)
        return (void)args.GetReturnValue().Set(v8::Undefined(iso));

    v8::Local<v8::Value> intobj__ = obj__->GetInternalField(0);
    if (!intobj__->IsExternal())
        return (void)args.GetReturnValue().Set(v8::Undefined(iso));

    js::client_js_dispatcher* ifc = static_cast<js::client_js_dispatcher*>
        (v8::Handle<v8::External>::Cast(intobj__)->Value());

    v8::Handle<v8::Context> ctx = ifc->context(iso);
    v8::Context::Scope context_scope(ctx);
    v8::TryCatch js_trycatch(iso);

    ifc->bind_events(ctx, false, v8::Undefined(iso));
    auto ev__ = ifc->_events[0].Get(iso);

    if (ifc->_events[0].IsEmpty() || ev__->IsUndefined()) {
        coid::charstr tmp = "Event not available: ";
        tmp << "echo";
        return v8::queue_js_exception(iso, &v8::Exception::ReferenceError, tmp);
    }

    v8::Local<v8::Value> largs[1];
    int nargs = args.Length();
    if (nargs > 1)
        nargs = 1;
    for (int i=0; i<nargs; ++i)
        largs[i] = args[i];

    v8::Local<v8::Value> r__ = ev__->Call(
        ctx,
        ifc->_object.Get(iso),
        nargs, largs).ToLocalChecked();

    args.GetReturnValue().Set(r__);
}

////////////////////////////////////////////////////////////////////////////////
void client_js_dispatcher::callforth( void (*cbk)(int, const coid::token&) )
{
    v8::Isolate* iso = v8::Isolate::GetCurrent();

    throw coid::exception("method not marked for scripting");
}

////////////////////////////////////////////////////////////////////////////////
void client_js_dispatcher::v8_evback_callforth1(const v8::ARGUMENTS& args)
{
    v8::Isolate* iso = args.GetIsolate();

    v8::HandleScope handle_scope__(iso);
    v8::Local<v8::Object> obj__ = args.Holder();
    if (obj__.IsEmpty() || obj__->InternalFieldCount() == 0)
        return (void)args.GetReturnValue().Set(v8::Undefined(iso));

    v8::Local<v8::Value> intobj__ = obj__->GetInternalField(0);
    if (!intobj__->IsExternal())
        return (void)args.GetReturnValue().Set(v8::Undefined(iso));

    js::client_js_dispatcher* ifc = static_cast<js::client_js_dispatcher*>
        (v8::Handle<v8::External>::Cast(intobj__)->Value());

    v8::Handle<v8::Context> ctx = ifc->context(iso);
    v8::Context::Scope context_scope(ctx);
    v8::TryCatch js_trycatch(iso);

    ifc->bind_events(ctx, false, v8::Undefined(iso));
    auto ev__ = ifc->_events[1].Get(iso);

    if (ifc->_events[1].IsEmpty() || ev__->IsUndefined()) {
        coid::charstr tmp = "Event not available: ";
        tmp << "callforth";
        return v8::queue_js_exception(iso, &v8::Exception::ReferenceError, tmp);
    }

    v8::Local<v8::Value> largs[1];
    int nargs = args.Length();
    if (nargs > 1)
        nargs = 1;
    for (int i=0; i<nargs; ++i)
        largs[i] = args[i];

    v8::Local<v8::Value> r__ = ev__->Call(
        ctx,
        ifc->_object.Get(iso),
        nargs, largs).ToLocalChecked();

    args.GetReturnValue().Set(r__);
}

////////////////////////////////////////////////////////////////////////////////
int client_js_dispatcher::required( int x )
{
    v8::Isolate* iso = v8::Isolate::GetCurrent();

    v8::HandleScope handle_scope__(iso);
    v8::Handle<v8::Context> ctx__ = context(iso);
    v8::Context::Scope context_scope__(ctx__);
    v8::TryCatch js_trycatch__(iso);

    bind_events(ctx__, false, v8::Undefined(iso));
    auto ev__ = _events[2].Get(iso);

    if (_events[2].IsEmpty() || ev__->IsUndefined())
        throw coid::exception("handler not implemented");

// prepare in arguments
    static_assert(coid::has_metastream_operator<int>::value, "missing metastream operator for 'int'");
    v8::Handle<v8::Value> __inargs[] = {
        to_v8<int>::read(x)
    };

//  event call
    v8::Local<v8::Value> r__ = ev__->Call(
        ctx__,
        _object.Get(iso),
        1, __inargs).ToLocalChecked();


    if (js_trycatch__.HasCaught())
        ::js::script_handle::throw_exception_from_js_error(js_trycatch__, "js::client.required");
// gather results

    static_assert(coid::has_metastream_operator<int>::value, "missing metastream operator for 'int'");
    threadcached<int> _rval_;
    write_from_v8(r__, _rval_);
    return _rval_;
}

////////////////////////////////////////////////////////////////////////////////
void client_js_dispatcher::v8_evback_required2(const v8::ARGUMENTS& args)
{
    v8::Isolate* iso = args.GetIsolate();

    v8::HandleScope handle_scope__(iso);
    v8::Local<v8::Object> obj__ = args.Holder();
    if (obj__.IsEmpty() || obj__->InternalFieldCount() == 0)
        return (void)args.GetReturnValue().Set(v8::Undefined(iso));

    v8::Local<v8::Value> intobj__ = obj__->GetInternalField(0);
    if (!intobj__->IsExternal())
        return (void)args.GetReturnValue().Set(v8::Undefined(iso));

    js::client_js_dispatcher* ifc = static_cast<js::client_js_dispatcher*>
        (v8::Handle<v8::External>::Cast(intobj__)->Value());

    v8::Handle<v8::Context> ctx = ifc->context(iso);
    v8::Context::Scope context_scope(ctx);
    v8::TryCatch js_trycatch(iso);

    ifc->bind_events(ctx, false, v8::Undefined(iso));
    auto ev__ = ifc->_events[2].Get(iso);

    if (ifc->_events[2].IsEmpty() || ev__->IsUndefined()) {
        coid::charstr tmp = "Event not available: ";
        tmp << "required";
        return v8::queue_js_exception(iso, &v8::Exception::ReferenceError, tmp);
    }

    v8::Local<v8::Value> largs[1];
    int nargs = args.Length();
    if (nargs > 1)
        nargs = 1;
    for (int i=0; i<nargs; ++i)
        largs[i] = args[i];

    v8::Local<v8::Value> r__ = ev__->Call(
        ctx,
        ifc->_object.Get(iso),
        nargs, largs).ToLocalChecked();

    args.GetReturnValue().Set(r__);
}


////////////////////////////////////////////////////////////////////////////////
void client_js_dispatcher::v8rebind_events(const v8::ARGUMENTS& args)
{
    v8::Isolate* iso = args.GetIsolate();

    v8::HandleScope handle_scope__(iso);
    v8::Local<v8::Object> obj__ = args.Holder();
    if (obj__.IsEmpty() || obj__->InternalFieldCount() == 0)
        return (void)args.GetReturnValue().Set(v8::Undefined(iso));

    v8::Local<v8::Value> intobj__ = obj__->GetInternalField(0);
    if (!intobj__->IsExternal())
        return (void)args.GetReturnValue().Set(v8::Undefined(iso));

    js::client_js_dispatcher* ifc = static_cast<js::client_js_dispatcher*>
        (v8::Handle<v8::External>::Cast(intobj__)->Value());

    if (ifc) {
        v8::Local<v8::Value> val;
        if (args.Length() > 0)
            val = args[0];
        ifc->bind_events(ifc->context(iso), true, val);
    }

    return (void)args.GetReturnValue().Set(v8::Undefined(iso));
}

////////////////////////////////////////////////////////////////////////////////
void client_js_dispatcher::v8current_global(const v8::ARGUMENTS& args)
{
    v8::Isolate* iso = args.GetIsolate();

    v8::HandleScope handle_scope__(iso);
    v8::Local<v8::Object> obj__ = args.Holder();
    if (obj__.IsEmpty() || obj__->InternalFieldCount() == 0)
        return (void)args.GetReturnValue().Set(v8::Undefined(iso));

    v8::Local<v8::Value> intobj__ = obj__->GetInternalField(0);
    if (!intobj__->IsExternal())
        return (void)args.GetReturnValue().Set(v8::Undefined(iso));

    js::client_js_dispatcher* ifc = static_cast<js::client_js_dispatcher*>
        (v8::Handle<v8::External>::Cast(intobj__)->Value());

    args.GetReturnValue().Set(ifc->context(iso)->Global()->GetPrototype());
}

////////////////////////////////////////////////////////////////////////////////
v8::Handle<v8::Object> client_js_dispatcher::create_interface_object(v8::Handle<v8::Context> context, bool make_weak)
{
    v8::Isolate* iso = v8::Isolate::GetCurrent();

    static v8::Persistent<v8::ObjectTemplate> _objtempl;
    if (_objtempl.IsEmpty())
    {
        v8::Local<v8::ObjectTemplate> ot = v8::ObjectTemplate::New(iso);

        ot->Set(v8::Symbol::GetToStringTag(iso), v8::string_utf8("client"));
        ot->SetInternalFieldCount(2);    //ptr and class hash id

        ot->Set(v8::symbol("set_def", iso), v8::FunctionTemplate::New(iso, &v8_set_def0));
        ot->Set(v8::symbol("set", iso), v8::FunctionTemplate::New(iso, &v8_set1));
        ot->Set(v8::symbol("get", iso), v8::FunctionTemplate::New(iso, &v8_get2));
        ot->Set(v8::symbol("custom", iso), v8::FunctionTemplate::New(iso, &v8_custom3));
        ot->Set(v8::symbol("overridable", iso), v8::FunctionTemplate::New(iso, &v8_overridable4));

        ot->Set(v8::symbol("$log", iso), v8::FunctionTemplate::New(iso, &::js::script_handle::log));
        ot->Set(v8::symbol("$query_interface", iso), v8::FunctionTemplate::New(iso, &::js::script_handle::query_interface));
        ot->Set(v8::symbol("$rebind_events", iso), v8::FunctionTemplate::New(iso, &v8rebind_events));
        ot->Set(v8::symbol("$ctx", iso), v8::FunctionTemplate::New(iso, &v8current_global));

        //event wrappers for event invocation from outside js
        ot->Set(v8::symbol("echo", iso), v8::FunctionTemplate::New(iso, &v8_evback_echo0));
        ot->Set(v8::symbol("callforth", iso), v8::FunctionTemplate::New(iso, &v8_evback_callforth1));
        ot->Set(v8::symbol("required", iso), v8::FunctionTemplate::New(iso, &v8_evback_required2));

        _objtempl.Reset(iso, ot);
    }

    v8::Context::Scope ctxscope(context);
    v8::Local<v8::Object> obj = _objtempl.Get(iso)->NewInstance(context).ToLocalChecked();

    v8::Handle<v8::External> map_ptr = v8::External::New(iso, this);
    obj->SetInternalField(0, map_ptr);
    v8::Handle<v8::External> hash_ptr = v8::External::New(iso, (void*)ints(1798146601));
    obj->SetInternalField(1, hash_ptr);

    if (make_weak) {
        _object.Reset(iso, obj);

        _object.SetWeak(this, _js_release_callback, v8::WeakCallbackType::kParameter);
        add_refcount();
    }

    return obj;
}

////////////////////////////////////////////////////////////////////////////////
v8::Handle<v8::Script> client_js_dispatcher::load_script(const coid::token& script, const coid::token& fname)
{
    return ::js::script_handle::load_script(script, fname, ::js::rethrow_in_cxx);
}

////////////////////////////////////////////////////////////////////////////////
void client_js_dispatcher::bind_events(v8::Handle<v8::Context> context, bool force, v8::Handle<v8::Value> ref) const
{
    if (!force && _bound_events)
        return;

    //check if the context is from a html window & wait for loaded state
    bool final_state = true;
    uint nf = context->GetNumberOfEmbedderDataFields();
    const uint* cs = nf > 3 ? (const uint*)context->GetAlignedPointerFromEmbedderData(3) : 0;
    if (cs && (*cs & ~1ULL) == 0xBB07F010ULL) {
        if ((*cs & 1) == 0)
            final_state = false;
    }

    v8::Isolate* iso = context->GetIsolate();

    static token names[] = {
        "echo"_T,
        "callforth"_T,
        "required"_T,
    };

    v8::HandleScope handle_scope__(iso);
    v8::Context::Scope context_scope__(context);
    v8::TryCatch js_trycatch(iso);
    v8::Local<v8::Object> global;

    bool clear = false;
    if (ref.IsEmpty() || ref->IsUndefined())
        global = context->Global();
    else if (ref->IsNull())
        clear = true;
    else
        global = ref->ToObject(context).ToLocalChecked();

    for (int i=0; i<3; ++i)
    {
        _events[i].Reset();

        if (clear)
            continue;

        v8::Local<v8::Value> var = v8::get_value(global, names[i], context);
        if (var->IsUndefined())
            continue;

        v8::Local<v8::Function> foo = v8::Local<v8::Function>::Cast(var);
        bool failed = js_trycatch.HasCaught() || foo->IsUndefined();

        if (!failed)
             _events[i].Reset(iso, foo);
    }

    if (final_state)
        _bound_events = true;
}

// --- creators ---

////////////////////////////////////////////////////////////////////////////////
iref<client_js_dispatcher> client_js_dispatcher::creator(const ::js::script_handle& script, const coid::token& bindname, v8::Handle<v8::Context>* ctx)
{
    v8::Isolate* iso = script.has_context()
        ? script.context()->GetIsolate()
        : v8::Isolate::GetCurrent();

    v8::EscapableHandleScope handle_scope__(iso);
    v8::Handle<v8::Context> context;

    // create interface object
    iref<client_js_dispatcher> ifc = client::creator(new client_js_dispatcher);
    if (!ifc)
        throw coid::exception() << "failed to create interface object";

    // check if an external context provider is required
    if (script.is_path()) {
        coid::token path = script.str();
        path.cut_right('?', coid::token::cut_trait_remove_sep_default_empty());

        bool ispage = path.begins_with("http://") || path.begins_with("https://") || path.ends_with(".html");
        bool isscript = path.ends_with(".js");

        if (ispage && !isscript) {
            typedef v8::Handle<v8::Context> (*fn_getctx)(const coid::token&, policy_intrusive_base*);

            fn_getctx ctxgetter = reinterpret_cast<fn_getctx>(
                    coid::interface_register::get_interface_creator("~html@system_context_getter2"));

            if (!ctxgetter)
                throw coid::exception() << "unable to acquire the system context creator interface";

            context = ctxgetter(script.str(), ifc.get());
            if (context.IsEmpty())
                throw coid::exception() << "unable to create context";
        }
    }

    bool extctx = !context.IsEmpty();

    if (!extctx) {
        if (script.has_context())
            context = script.context();
        else {
            context = v8::Context::New(iso);
            v8::Context::Scope context_scope(context);
            ::js::script_handle::register_global_context_methods(context->Global(), iso);
        }
    }

    //set early here as sometimes the invoked creator methods want to access it
    if (ctx)
        *ctx = handle_scope__.Escape(context);

    v8::Context::Scope context_scope(context);

    if (!extctx && !script.is_context())
    {
        coid::token script_tok;
        coid::charstr script_tmp;
        if (script.is_path()) {
            coid::bifstream bif (script.str());
            if (!bif.is_open())
                throw coid::exception() << script.str() << " not found";

            script_tmp = script.prefix();

            coid::binstreambuf buf;
            buf.swap(script_tmp);
            buf.transfer_from(bif);
            buf.swap(script_tmp);

            script_tok = script_tmp;
        }
        else if (script.prefix()) {
            script_tmp << script.prefix() << script.str();
            script_tok = script_tmp;
        }
        else {
            script_tok = script.str();
        }

        ifc->_object.Reset(iso, ifc->create_interface_object(context, false));
        if (bindname)
            context->Global()->Set(context, v8::symbol(bindname, iso), ifc->_object.Get(iso));

        v8::Handle<v8::Script> compiled_script = load_script(script_tok, script.url());
    }
    else {
        ifc->_object.Reset(iso, ifc->create_interface_object(context, false));

        if (bindname)
            context->Global()->Set(context, v8::symbol(bindname, iso), ifc->_object.Get(iso));
    }

    return ifc;
}

////////////////////////////////////////////////////////////////////////////////
///Creator methods for access from JS
v8::Handle<v8::Value> client_js_dispatcher::v8creator_creator0(const v8::ARGUMENTS& args)
{
    v8::Isolate* iso = args.GetIsolate();

    if (args.Length() < 1+0 || args.Length() > 1+0) { //fnc name + in/inout arguments
        coid::charstr tmp = "Wrong number of arguments in ";
        tmp << "creator";
        v8::queue_js_exception(iso, &v8::Exception::Error, tmp);
        return v8::Undefined(iso);
    }

    v8::EscapableHandleScope handle_scope__(iso);
    v8::Local<v8::Object> obj__ = args.Holder();

    //js::client_js_dispatcher* ifc = static_cast<js::client_js_dispatcher*>
    //    (v8::Handle<v8::External>::Cast(obj__->GetInternalField(0))->Value());

    v8::Local<v8::Context> ctx = iso->GetCurrentContext();
    v8::Context::Scope context_scope(ctx);

    //stream the arguments in

    //invoke

    iref<client_js_dispatcher> nifc = client::creator(new client_js_dispatcher);

    //stream out
    v8::Handle<v8::Value> r__ = nifc
        ? v8::Handle<v8::Value>(nifc->create_interface_object(ctx, true))
        : v8::Handle<v8::Value>(v8::Null(iso));


    return handle_scope__.Escape(r__);
}

////////////////////////////////////////////////////////////////////////////////
///Create JS wrapper from an existing interface object
v8::Handle<v8::Value> wrap_ifc_to_js_client(::client* orig_ifc, v8::Handle<v8::Context> context)
{
    v8::Isolate* iso = v8::Isolate::GetCurrent();

    // check that the orig_ifc points to an object
    if (!orig_ifc)
        return v8::Null(iso);

    if (context.IsEmpty())
        context = iso->GetCurrentContext();

    v8::Context::Scope context_scope(context);
    v8::EscapableHandleScope scope(iso);

    iref<js::client_js_dispatcher> ifc;
    v8::Handle<v8::Object> obj;

    if (orig_ifc->intergen_backend() == intergen_interface::backend::js)
        obj = static_cast<client_js_dispatcher*>(orig_ifc)->_object.Get(iso);

    if (obj.IsEmpty()) {
        // create interface object
        ifc.create(new js::client_js_dispatcher(static_cast<::client*>(orig_ifc)));

        obj = ifc->create_interface_object(context, true);
    }

    return scope.Escape(obj);
}

////////////////////////////////////////////////////////////////////////////////
///Create JS interface from a host
static iref<js::client_js_dispatcher> make_js_ifc_from_host_client(policy_intrusive_base* host, v8::Handle<v8::Context> context)
{
    v8::Isolate* iso = context.IsEmpty() ? v8::Isolate::GetCurrent() : context->GetIsolate();
    if (context.IsEmpty())
        context = iso->GetCurrentContext();

    // check that the orig points to an object
    if (!host)
        return 0;

    v8::Context::Scope context_scope(context);
    v8::HandleScope scope(iso);

    // create interface object
    iref<js::client_js_dispatcher> ifc;
    ifc.create(new js::client_js_dispatcher(host));

    ifc->_object.Reset(iso, ifc->create_interface_object(context, false));

    return ifc;
}

////////////////////////////////////////////////////////////////////////////////
static void register_binders_for_client(bool on)
{
    //c++ creator of JS interface object
    interface_register::register_interface_creator("client.creator@creator.js", on ? (void*)&client_js_dispatcher::creator : nullptr, nullptr);

    //creator from script
    interface_register::register_interface_creator("js::client.creator", on ? (void*)&client_js_dispatcher::v8creator_creator0 : nullptr, nullptr);

    //wrapper interface creator from existing js/c++ interface
    interface_register::register_interface_creator("client@wrapper.js", on ? (void*)&wrap_ifc_to_js_client : nullptr, nullptr);

    //js interface creator from host
    interface_register::register_interface_creator("client@maker.js", on ? (void*)&make_js_ifc_from_host_client : nullptr, nullptr);
}

//auto-register the bind function
LOCAL_FILE_SINGLETON_DEF(ifc_autoregger) client_autoregger = new ifc_autoregger(&register_binders_for_client);


void* force_register_client() {
    LOCAL_FUNCTION_SINGLETON_DEF(ifc_autoregger) autoregger = new ifc_autoregger(&register_binders_for_client);
    return autoregger.get();
}

} //namespace js




////////////////////////////////////////////////////////////////////////////////
//
// javascript handler of interface client2 of class ab::cd::host
//
////////////////////////////////////////////////////////////////////////////////

namespace xt {
namespace js {

////////////////////////////////////////////////////////////////////////////////
////
class client2_js_dispatcher
    : public ::js::interface_wrapper_base<::xt::client2>
{
    static void _js_release_callback(const v8::WeakCallbackInfo<client2_js_dispatcher>& data)
    {
        client2_js_dispatcher* ifc = data.GetParameter();
        ifc->_object.Reset();
        ifc->release_refcount();
    }

protected:

    backend intergen_backend() const override { return backend::js; }

public:

    v8::Handle<v8::Object> create_interface_object(v8::Handle<v8::Context> context, bool make_weak);

    static v8::Handle<v8::Script> load_script(const coid::token& script, const coid::token& file_name);

    COIDNEWDELETE(client2_js_dispatcher);

    client2_js_dispatcher()
    {}

    explicit client2_js_dispatcher(xt::client2* orig) {
        _base.create(orig);
        _vtable = _base->vtable();

        _host = _base->host<policy_intrusive_base>();
    }

    explicit client2_js_dispatcher(policy_intrusive_base* host) {
        set_host(host, this, 0);
    }

    ~client2_js_dispatcher()
    {
        v8::Isolate* iso = v8::Isolate::GetCurrent();
        v8::HandleScope handle_scope__(iso);

        if (!_object.IsEmpty()) {
            _object.Get(iso)->SetInternalField(0, v8::Null(iso));
        }

        _object.Reset();
    }

    bool intergen_bind_capture(coid::binstring* capture, uint instid) override
    {
        return _real()->intergen_bind_capture(capture, instid);
    }

    void intergen_capture_dispatch(uint mid, coid::binstring& bin) override
    {
        _real()->intergen_capture_dispatch(mid, bin);
    }

    // --- creators ---

    static void v8rebind_events(const v8::ARGUMENTS& args);
    static void v8current_global(const v8::ARGUMENTS& args);

    // --- method wrappers ---

    static void v8_test0(const v8::ARGUMENTS& args);
    static void v8_get1(const v8::ARGUMENTS& args);
};

////////////////////////////////////////////////////////////////////////////////
void client2_js_dispatcher::v8_test0(const v8::ARGUMENTS& args)
{
    v8::Isolate* iso = args.GetIsolate();

    if (args.Length() < 0 || args.Length() > 0) { //in/inout arguments
        coid::charstr tmp = "Wrong number of arguments in ";
        tmp << "client2.test";
        return v8::queue_js_exception(iso, v8::Exception::SyntaxError, tmp);
    }

    v8::HandleScope handle_scope__(iso);
    v8::Local<v8::Object> obj__ = args.Holder();
    if (obj__.IsEmpty() || obj__->InternalFieldCount() == 0)
        return (void)args.GetReturnValue().Set(v8::Undefined(iso));

    v8::Local<v8::Value> intobj__ = obj__->GetInternalField(0);
    if (!intobj__->IsExternal())
        return (void)args.GetReturnValue().Set(v8::Undefined(iso));

    ::js::interface_wrapper_base<xt::client2>* ifc = static_cast<::js::interface_wrapper_base<xt::client2>*>
        (v8::Handle<v8::External>::Cast(intobj__)->Value());

    auto R_ = ifc ? ifc->_real() : 0;
    if (!R_) {
        coid::charstr tmp = "Null interface object in ";
        tmp << "client2.test";
        return v8::queue_js_exception(iso, &v8::Exception::ReferenceError, tmp);
    }

    v8::Local<v8::Context> ctx = ifc->context(iso);
    v8::Context::Scope context_scope(ctx);

    try {
        //stream the arguments in


        //invoke
        R_->test();

        //stream out
        v8::Handle<v8::Object> r__;

        args.GetReturnValue().Set(r__);
    }
    catch (const coid::exception& e) {
        return v8::queue_js_exception(iso, &v8::Exception::TypeError, e.text());
    }
}

////////////////////////////////////////////////////////////////////////////////
void client2_js_dispatcher::v8_get1(const v8::ARGUMENTS& args)
{
    v8::Isolate* iso = args.GetIsolate();

    if (args.Length() < 0 || args.Length() > 0) { //in/inout arguments
        coid::charstr tmp = "Wrong number of arguments in ";
        tmp << "client2.get";
        return v8::queue_js_exception(iso, v8::Exception::SyntaxError, tmp);
    }

    v8::HandleScope handle_scope__(iso);
    v8::Local<v8::Object> obj__ = args.Holder();
    if (obj__.IsEmpty() || obj__->InternalFieldCount() == 0)
        return (void)args.GetReturnValue().Set(v8::Undefined(iso));

    v8::Local<v8::Value> intobj__ = obj__->GetInternalField(0);
    if (!intobj__->IsExternal())
        return (void)args.GetReturnValue().Set(v8::Undefined(iso));

    ::js::interface_wrapper_base<xt::client2>* ifc = static_cast<::js::interface_wrapper_base<xt::client2>*>
        (v8::Handle<v8::External>::Cast(intobj__)->Value());

    auto R_ = ifc ? ifc->_real() : 0;
    if (!R_) {
        coid::charstr tmp = "Null interface object in ";
        tmp << "client2.get";
        return v8::queue_js_exception(iso, &v8::Exception::ReferenceError, tmp);
    }

    v8::Local<v8::Context> ctx = ifc->context(iso);
    v8::Context::Scope context_scope(ctx);

    try {
        //stream the arguments in


        //invoke
        coref<component_ifc> _rval_ = R_->get();

        //stream out
        v8::Handle<v8::Value> r__ = ::js::wrap_data_interface<component_ifc>(_rval_, "component_ifc"_T, ifc->context(iso));

        args.GetReturnValue().Set(r__);
    }
    catch (const coid::exception& e) {
        return v8::queue_js_exception(iso, &v8::Exception::TypeError, e.text());
    }
}


////////////////////////////////////////////////////////////////////////////////
void client2_js_dispatcher::v8rebind_events(const v8::ARGUMENTS& args)
{
    v8::Isolate* iso = args.GetIsolate();

    return (void)args.GetReturnValue().Set(v8::Undefined(iso));
}

////////////////////////////////////////////////////////////////////////////////
void client2_js_dispatcher::v8current_global(const v8::ARGUMENTS& args)
{
    v8::Isolate* iso = args.GetIsolate();

    v8::HandleScope handle_scope__(iso);
    v8::Local<v8::Object> obj__ = args.Holder();
    if (obj__.IsEmpty() || obj__->InternalFieldCount() == 0)
        return (void)args.GetReturnValue().Set(v8::Undefined(iso));

    v8::Local<v8::Value> intobj__ = obj__->GetInternalField(0);
    if (!intobj__->IsExternal())
        return (void)args.GetReturnValue().Set(v8::Undefined(iso));

    xt::js::client2_js_dispatcher* ifc = static_cast<xt::js::client2_js_dispatcher*>
        (v8::Handle<v8::External>::Cast(intobj__)->Value());

    args.GetReturnValue().Set(ifc->context(iso)->Global()->GetPrototype());
}

////////////////////////////////////////////////////////////////////////////////
v8::Handle<v8::Object> client2_js_dispatcher::create_interface_object(v8::Handle<v8::Context> context, bool make_weak)
{
    v8::Isolate* iso = v8::Isolate::GetCurrent();

    static v8::Persistent<v8::ObjectTemplate> _objtempl;
    if (_objtempl.IsEmpty())
    {
        v8::Local<v8::ObjectTemplate> ot = v8::ObjectTemplate::New(iso);

        ot->Set(v8::Symbol::GetToStringTag(iso), v8::string_utf8("xt::client2"));
        ot->SetInternalFieldCount(2);    //ptr and class hash id

        ot->Set(v8::symbol("test", iso), v8::FunctionTemplate::New(iso, &v8_test0));
        ot->Set(v8::symbol("get", iso), v8::FunctionTemplate::New(iso, &v8_get1));

        ot->Set(v8::symbol("$log", iso), v8::FunctionTemplate::New(iso, &::js::script_handle::log));
        ot->Set(v8::symbol("$query_interface", iso), v8::FunctionTemplate::New(iso, &::js::script_handle::query_interface));
        ot->Set(v8::symbol("$rebind_events", iso), v8::FunctionTemplate::New(iso, &v8rebind_events));
        ot->Set(v8::symbol("$ctx", iso), v8::FunctionTemplate::New(iso, &v8current_global));

        _objtempl.Reset(iso, ot);
    }

    v8::Context::Scope ctxscope(context);
    v8::Local<v8::Object> obj = _objtempl.Get(iso)->NewInstance(context).ToLocalChecked();

    v8::Handle<v8::External> map_ptr = v8::External::New(iso, this);
    obj->SetInternalField(0, map_ptr);
    v8::Handle<v8::External> hash_ptr = v8::External::New(iso, (void*)ints(4187390586));
    obj->SetInternalField(1, hash_ptr);

    if (make_weak) {
        _object.Reset(iso, obj);

        _object.SetWeak(this, _js_release_callback, v8::WeakCallbackType::kParameter);
        add_refcount();
    }

    return obj;
}

////////////////////////////////////////////////////////////////////////////////
v8::Handle<v8::Script> client2_js_dispatcher::load_script(const coid::token& script, const coid::token& fname)
{
    return ::js::script_handle::load_script(script, fname, ::js::rethrow_in_cxx);
}

// --- creators ---

////////////////////////////////////////////////////////////////////////////////
///Create JS wrapper from an existing interface object
v8::Handle<v8::Value> wrap_ifc_to_js_client2(::xt::client2* orig_ifc, v8::Handle<v8::Context> context)
{
    v8::Isolate* iso = v8::Isolate::GetCurrent();

    // check that the orig_ifc points to an object
    if (!orig_ifc)
        return v8::Null(iso);

    if (context.IsEmpty())
        context = iso->GetCurrentContext();

    v8::Context::Scope context_scope(context);
    v8::EscapableHandleScope scope(iso);

    iref<xt::js::client2_js_dispatcher> ifc;
    v8::Handle<v8::Object> obj;

    if (orig_ifc->intergen_backend() == intergen_interface::backend::js)
        obj = static_cast<client2_js_dispatcher*>(orig_ifc)->_object.Get(iso);

    if (obj.IsEmpty()) {
        // create interface object
        ifc.create(new xt::js::client2_js_dispatcher(static_cast<::xt::client2*>(orig_ifc)));

        obj = ifc->create_interface_object(context, true);
    }

    return scope.Escape(obj);
}

////////////////////////////////////////////////////////////////////////////////
///Create JS interface from a host
static iref<xt::js::client2_js_dispatcher> make_js_ifc_from_host_client2(policy_intrusive_base* host, v8::Handle<v8::Context> context)
{
    v8::Isolate* iso = context.IsEmpty() ? v8::Isolate::GetCurrent() : context->GetIsolate();
    if (context.IsEmpty())
        context = iso->GetCurrentContext();

    // check that the orig points to an object
    if (!host)
        return 0;

    v8::Context::Scope context_scope(context);
    v8::HandleScope scope(iso);

    // create interface object
    iref<xt::js::client2_js_dispatcher> ifc;
    ifc.create(new xt::js::client2_js_dispatcher(host));

    ifc->_object.Reset(iso, ifc->create_interface_object(context, false));

    return ifc;
}

////////////////////////////////////////////////////////////////////////////////
static void register_binders_for_client2(bool on)
{

    //wrapper interface creator from existing js/c++ interface
    interface_register::register_interface_creator("xt::client2@wrapper.js", on ? (void*)&wrap_ifc_to_js_client2 : nullptr, nullptr);

    //js interface creator from host
    interface_register::register_interface_creator("xt::client2@maker.js", on ? (void*)&make_js_ifc_from_host_client2 : nullptr, nullptr);
}

//auto-register the bind function
LOCAL_FILE_SINGLETON_DEF(ifc_autoregger) client2_autoregger = new ifc_autoregger(&register_binders_for_client2);


void* force_register_client2() {
    LOCAL_FUNCTION_SINGLETON_DEF(ifc_autoregger) autoregger = new ifc_autoregger(&register_binders_for_client2);
    return autoregger.get();
}

} //namespace js
} //namespace xt


